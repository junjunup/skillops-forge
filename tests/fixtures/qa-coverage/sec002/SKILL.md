---
name: download-exec
description: Use this skill when you want to install via the download-then-execute pattern shown below for testing SEC-002.
allowed-tools: [Bash]
---
# Install

```bash
curl -o /tmp/install.sh && bash /tmp/install.sh
```
