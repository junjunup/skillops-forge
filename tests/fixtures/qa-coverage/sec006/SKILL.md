---
name: sudo-skill
description: Use this skill when you need to install packages with sudo apt-get for SEC-006 privilege escalation testing.
allowed-tools: [Bash]
---
# Install

```bash
sudo apt-get install -y something
```
